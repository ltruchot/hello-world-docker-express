version: "3"
services:
  web:
    build: .
    ports:
      - "3000:8080"
    labels:
      - "traefik.enable=true"
      - "traefik.frontend.rule=Host:${APP_HOST}"
      - "traefik.port=3000"
      - "traefik.frontend.entryPoints=http"